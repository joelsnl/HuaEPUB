# Dev / CI extras. Runtime deps stay in requirements.txt.
# Install: pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
ruff==0.12.11

# Pin matches .github/workflows/release.yml and build.py fallback.
# Keep pyinstaller-hooks-contrib in the same major generation (pip pulls it in).
pyinstaller==6.22.2
